int computeBattleResult(Army* attackArmies, int attackSize, Army* defenseArmies, int defenseSize, int battleType, battleBuff) {
    initArmyBattleInfo(attackArmys, newAttackSoldiers, [], true, attackBuildingBuff, battleBuff.attackerPlusBuff);
    initArmyBattleInfo(defenseArmys, newDefenseSoldiers, [], false, defenseBuildingBuff, battleBuff.defenserPlusBuff);
    for(var waveIndex=0; waveIndex<20; ++waveIndex){
        
    }
}
